package com.workout.algorithms.algorithms;

public class BubbleSorter {
    private Object[] mObjects;

    public BubbleSorter(Object[] arrayToSort) {
        if(arrayToSort == null) {
            mObjects = new Object[0];
        } else {
            mObjects = arrayToSort;
        }
    }

    public void performSorting() {

    }
}
